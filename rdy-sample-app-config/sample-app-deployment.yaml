---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rdy-sample-app
  namespace: rdy-sample-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rdy-sample-app
  template:
    metadata:
      labels:
        app: rdy-sample-app
    spec:
      containers:
        - name: rdy-sample-app
          image: "quay.io/btomlins/greet:v0.0.1"
          command:
            - '/usr/bin/greet'
          args:
            - 'Hello participant\!'
...
